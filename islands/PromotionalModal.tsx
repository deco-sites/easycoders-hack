import Component from "$store/components/ui/PromotionalModal.tsx";
import type { Props } from "$store/components/ui/PromotionalModal.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
